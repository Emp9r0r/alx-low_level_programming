#include "main.h"

/**
 * _isdigit - The checks for a digit (0 through 9)
 * @f: The int to be checked
 * Return: 1 if f is a digit, 0 otherwise
 */
int _isdigit(int f)
{
	return (f >= '0' && f <= '9');
}
