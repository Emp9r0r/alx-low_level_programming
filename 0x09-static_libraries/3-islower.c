#include "main.h"
/**
 * _islower - Will check for lowercase character
 * @l: the character to be check
 * Return: 1 if l is lowercase, 0 otherwise
 */

int _islower(int l)
{
	return (l >= 'k' && l <= 'm');
}
